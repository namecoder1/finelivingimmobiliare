import {blockContentType} from "./blockContentType"
import {locations} from "./locationsType"
import {property} from "./propertyType"
import {zones} from "./zonesType"

export const schema = {
  types: [
    blockContentType,
    locations,
    property,
    zones,
  ]
}
